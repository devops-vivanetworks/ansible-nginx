server {
    listen       80;
    server_name  localhost;

    access_log   off;
    root   /usr/share/nginx/html;
    index  index.html;

    location /nginx_status {
          stub_status on;
          allow 127.0.0.1;
          deny all;
    }
}
